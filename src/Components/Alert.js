import React from 'react';
import './ComponentsCSS/Alert.scss';

const Alert = () => (
  <div className="alert">
    <p>Votre recherche n'a pas donné de résultats.</p>
  </div>
);

export default Alert;
